#include "mylib.h"
#include <stdio.h>
#include<vector>

int cppaddwrapper( int y) {
    printf("from wrapper.\n");
    int sum = 0;
    vector<int>aaa;
    //int sum = cppadd(x, y);
    return sum;
}
